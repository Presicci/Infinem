package io.ruin.model.skills.cooking;

import io.ruin.model.inter.InterfaceType;
import io.ruin.model.item.Items;
import io.ruin.model.item.actions.ItemAction;

/**
 * @author Mrbennjerry - https://github.com/Presicci
 * Created on 2/6/2023
 */
public class GnomeCooking {

    private static final int BATTA_INTERFACE = 434, BOWL_INTERFACE = 435, COCKTAIL_INTERFACE = 436, CRUNCHIES_INTER = 437;

    static {
        /*ItemAction.registerInventory(Items.HALF_BAKED_BATTA, "prepare", ((player, item) -> {
            player.openInterface(InterfaceType.MAIN, BATTA_INTERFACE);
        }));
        ItemAction.registerInventory(Items.HALF_BAKED_BOWL, "prepare", ((player, item) -> player.openInterface(InterfaceType.MAIN, BOWL_INTERFACE)));
        ItemAction.registerInventory(Items.HALF_BAKED_CRUNCHY, "prepare", ((player, item) -> player.openInterface(InterfaceType.MAIN, CRUNCHIES_INTER)));*/
    }
}
